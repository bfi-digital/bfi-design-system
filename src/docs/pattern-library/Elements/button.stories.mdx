import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { Button } from "../../../components/Button/index"

<Meta title="2 Pattern Library/3 Elements/Button" component={Button} />

# Button

The button has multiple different themes that can be selected using the
`colorScheme` attribute. Any theme's background and foreground colours
can be inverted by setting the `contrast` property to `dark` or `light`.

## Examples

<Preview>
    <Story name="Primary">
        <div style={{padding: "20px"}}>
            <Button level={1} url="#1">Click me</Button>
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Primary, light contrast">
        <div style={{padding: "20px", background: "maroon"}}>
            <Button level={1} url="#1" contrast="light">Click me</Button>
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Secondary">
        <div style={{padding: "20px"}}>
            <Button level={2} url="#2">Click me</Button>
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Tertiary">
        <div style={{padding: "20px"}}>
            <Button level={3} url="#3">Click me</Button>
        </div>
    </Story>
</Preview>

## Props 

<Props of={Button} />