import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Grid , GridCard } from "../../../components/Grid/index"
import { FilmCard } from "../../../components/FilmGrid/FilmCard"

import filmData from "../../../components/FilmGrid/data.js"
const demoFilms = [];

<Meta title="2 Pattern Library/4 Slices/Generic Grid" component={Grid} />

# Film Grid
 
 
## Examples

<Preview>
    <Story name="Grid with film cards">
        <div style={{
            margin: "50px",
            maxWidth: "1000px"
        }}>
            <Grid scroll>
            {filmData.map(datum=>
                <FilmCard {...datum} inScroller />
            )}
                <GridCard inScroller>Generic grid card</GridCard>
            </Grid>
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Grid, not scrolling">
        <div style={{
            margin: "50px",
            maxWidth: "1000px"
        }}>
            <Grid scroll={false}>
            {filmData.map(datum=>
                <FilmCard {...datum} inScroller={false} />
            )}
            
                <GridCard inScroller>Generic grid card</GridCard>
            </Grid>
        </div>
    </Story>
</Preview>

## Props of each file

<Props of={Grid} />
