import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { FilmGrid } from "../../../components/FilmGrid/index"
import demoFilms from "../../../components/FilmGrid/data"

<Meta title="2 Pattern Library/4 Slices/Film Grid" component={FilmGrid} />

# Film Grid
 
 
## Examples

<Preview>
    <Story name="Film grid">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Film grid with CTA">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms} link={{url: "http://example.com", title:"Example CTA"}}/>
        </div>
    </Story>
</Preview>


<Preview>
    <Story name="Film grid 1">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms.slice(0,1)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Film grid 2">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms.slice(0,2)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Film grid 3 with CTA">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms.slice(0,3)} allFilmsUrl="http://example.com" />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Film grid 4">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms.slice(0,4)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Film grid 6">
        <div style={{
            padding: "5px",
            maxWidth: "1000px"
        }}>
            <FilmGrid films={demoFilms.slice(0,6)} />
        </div>
    </Story>
</Preview>


## Props of each file

<Props of={FilmGrid} />
