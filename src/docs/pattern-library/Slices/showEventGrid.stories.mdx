import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs"
import { ShowEventGrid } from "../../../components/ShowEventGrid/index"
import demoEvents from "../../../components/ShowEventGrid/data"

<Meta title="2 Pattern Library/4 Slices/Show Grid (Event)" component={ShowEventGrid} />

# Show Event Grid
 
 
## Examples

<Canvas>
    <Story name="Multiple Events in long list">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={[...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="Multiple Events in scroller">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid asScroller events={[...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents, ...demoEvents]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="1 Event">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={[demoEvents[0]]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="2 Events">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={demoEvents.slice(0, 2)} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="3 Events">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={demoEvents.slice(0, 3)} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="4 Events">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={[...demoEvents.slice(0, 4)]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="5 Events">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={[...demoEvents.slice(0, 5)]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="6 Events">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid events={[...demoEvents.slice(0, 6)]} />
        </div>
    </Story>
</Canvas>

<Canvas>
    <Story name="Events with favouriting example">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ShowEventGrid 
                events={[...demoEvents, ...demoEvents]}
                favouritable={true}
                onFavourite={e => console.log(e)} 
                onUnfavourite={e => console.log(e)} 
            />
        </div>
    </Story>
</Canvas>

## Props 

<ArgsTable of={ShowEventGrid} />
