import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { NewsletterSignUp } from "../../../components/NewsletterSignUpUserPreferenceCentre/index"

<Meta title="2 Pattern Library/4 Slices/Newsletter Sign Up (User preference centre)" component={NewsletterSignUp} />

# Single NewsletterSignUp
 
 
## Examples

<Preview>
    <Story name="Default example">
        <div style={{padding: "20px", maxWidth: "600px"}}>
        <NewsletterSignUp title="Sign up for mail bulletins, the latest in film and all the latest news" desc="Choose what interests you and never miss a thing." changePreferencesUrl="http://www.example.com" onSignUp={ (email) => alert(`Sign up requested for ${email}`)} />
        </div>
    </Story>
</Preview>


<Props of={NewsletterSignUp} />
