import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ArticleGrid } from "../../../components/ArticleGrid/index"
import demoArticles from "../../../components/ArticleGrid/data"

<Meta title="2 Pattern Library/Slices/Article Grid" component={ArticleGrid} />

# Article Grid
 
 
## Examples

<Preview>
    <Story name="Multiple Articles with first highlighted">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles.slice(0, 2)]} firstHighlighted />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Multiple Articles without first highlighted">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Multiple Articles limited to 3 with load more">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid limitArticles articles={[...demoArticles, ...demoArticles]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Multiple Articles with first highlighted no grey background">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles.slice(0, 2)]} firstHighlighted pageWithSideBar={true}>
                <ArticleGrid articles={false} skeletons={3} pageWithSideBar={true} />
            </ArticleGrid>
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Three Articles with title and CTA">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles]} optionalTitle="Latest articles" optionalCTALink="http://google.com" optionalCTATitle="View all articles" />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="1 Article">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[demoArticles[0]]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="2 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={demoArticles.slice(0, 2)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="3 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={demoArticles.slice(0, 3)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="4 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles.slice(0, 4)]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="5 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles.slice(0, 5)]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="6 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles.slice(0, 6)]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Loading">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={false} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Loading with limit">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={false} skeletons={3} />
        </div>
    </Story>
</Preview>


<Preview>
    <Story name="Example of loading state">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles.slice(0, 2)]} firstHighlighted pageWithSideBar={true}>
                <ArticleGrid articles={false} skeletons={3} pageWithSideBar={true} />
            </ArticleGrid>
        </div>
    </Story>
</Preview>

## Props 

<Props of={ArticleGrid} />
