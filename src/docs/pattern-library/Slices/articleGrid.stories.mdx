import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ArticleGrid } from "../../../components/ArticleGrid/index"
import demoArticles from "../../../components/ArticleGrid/data"

<Meta title="Pattern Library|Slices/Article Grid" component={ArticleGrid} />

# Sidebar Page Links
 
 
## Examples

<Preview>
    <Story name="Multiple Articles with first highlighted">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles]} firstHighlighted />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Multiple Articles without first highlighted">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles, ...demoArticles]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Three Articles with title and CTA">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles]} optionalTitle="Latest articles" optionalCTALink="http://google.com" optionalCTATitle="View all articles" />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="1 Article">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[demoArticles[0]]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="2 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={demoArticles.slice(0, 2)} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="3 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={demoArticles} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="4 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles.slice(0, 1)]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="5 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles.slice(0, 2)]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="6 Articles">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={[...demoArticles, ...demoArticles]} />
        </div>
    </Story>
</Preview>

<Preview>
    <Story name="Loading">
        <div style={{
            padding: "10px",
            maxWidth: "1000px",
            margin: "0 auto"
        }}>
            <ArticleGrid articles={false} />
        </div>
    </Story>
</Preview>

## Props 

<Props of={ArticleGrid} />
